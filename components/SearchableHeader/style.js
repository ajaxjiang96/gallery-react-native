import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  searchableHeader: {
    padding: 20,
    fontSize: 48,
    textAlign: 'center',
    color: '#999',
    fontWeight: '100',
  }
})